how_many = int(input("How many Fib numbers do you want to generate?"))
Fib = [1,1]
if how_many==1:
    Fib.remove(1)
elif how_many==2:
    pass
else:
    while len(Fib)<=how_many:
        Fib.append(Fib[-2]+Fib[-1])
print(Fib)
